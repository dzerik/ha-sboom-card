{
  "name": "ha-sboom-card",
  "version": "0.2.0",
  "description": "Компактная Lovelace-карточка плеера SberBoom для Home Assistant (no-build, Lit). Переиспользует компоненты и data-feed интеграции sboom_ha.",
  "private": true,
  "type": "module",
  "homepage": "https://github.com/dzerik/ha-sboom-card",
  "keywords": ["home-assistant", "lovelace", "sberboom", "lit", "hacs"],
  "license": "MIT",
  "scripts": {
    "check": "node --check <(cat sboom-card.js)",
    "lint": "prettier --check sboom-card.js",
    "format": "prettier --write sboom-card.js"
  },
  "devDependencies": {
    "prettier": "^3"
  }
}
